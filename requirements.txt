# ============================================
# Z-SCORE ICEBERG HUNTER - Requirements
# Production-Ready Crypto Futures Trading Bot
# Python 3.10+ Compatible
# ============================================

# -------------------- Core Data Science & ML --------------------
numpy==1.24.3
pandas==2.0.3
scipy==1.11.1

# PyTorch (CPU version - no CUDA needed for LSTM models)
torch==2.0.1
# Note: If you want explicit CPU build, use torch index:
# pip install torch==2.0.1 --index-url https://download.pytorch.org/whl/cpu

# -------------------- Networking & API --------------------
requests==2.31.0
urllib3==2.0.7

# -------------------- WebSocket (Real-time Data Streams) --------------------
websocket-client==1.6.1
python-socketio==5.9.0
python-engineio==4.8.0

# -------------------- Cryptography (ED25519 Signing) --------------------
cryptography==41.0.7

# -------------------- Environment & Configuration --------------------
python-dotenv==1.0.0

# -------------------- Excel Logging (Optional) --------------------
openpyxl==3.1.2

# -------------------- Date & Time Utilities --------------------
python-dateutil==2.8.2
pytz==2023.3

# -------------------- Telegram Bot (Notifications & Control) --------------------
# Note: Your code uses custom HTTP calls, not python-telegram-bot
# No additional package needed - uses urllib.request

# -------------------- Additional Utilities --------------------
# All other imports (logging, threading, time, json, collections, 
# dataclasses, typing, datetime, signal, sys) are Python built-ins

# ============================================
# Installation Instructions:
# ============================================
# Standard installation:
#   pip install -r requirements.txt
#
# With explicit PyTorch CPU:
#   pip install torch==2.0.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Docker installation (recommended):
#   FROM python:3.10-slim
#   COPY requirements.txt .
#   RUN pip install --no-cache-dir -r requirements.txt
# ============================================
